<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FFFFFF">

    <!-- Encabezado (idéntico al original con menú, logo y perfil) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:background="#002326"
        android:gravity="center_vertical"
        android:weightSum="3">

        <!-- Icono de menú (tres rayitas) -->
        <ImageView
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="0.5"
            android:src="@drawable/menu"
        android:layout_marginEnd="8dp"
        android:contentDescription="Menú"/>

        <!-- Título "PanascOOP" centrado -->
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:text="PanascOOP"
            android:textSize="24sp"
            android:textColor="#fff"
            android:textStyle="bold"
            android:gravity="center"/>

        <!-- Sección de perfil (icono + texto) -->
        <LinearLayout
            android:layout_width="83dp"
            android:layout_height="28dp"
            android:layout_weight="0.5"
            android:gravity="center_vertical|end"
            android:orientation="horizontal">

            <ImageView
                android:layout_width="wrap_content"
                android:layout_height="40dp"
                android:layout_marginEnd="8dp"
                android:contentDescription="Usuario"
                android:src="@drawable/user" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Perfil"
                android:textColor="#C3C3C3"
                android:textSize="16sp" />
        </LinearLayout>
    </LinearLayout>

    <!-- Resto del formulario (se mantiene igual) -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp">

            <!-- ... (todo el contenido del formulario permanece igual) ... -->

        </LinearLayout>
    </ScrollView>
</LinearLayout>